# 工程规范审查记录

日期：2026-05-04

依据：`Docs/Godot工程通用要求.md`

## 已整改项

- 场景优先：
  - 将推进器内部固定视觉结构从脚本动态创建改为 `scenes/rov/thruster.tscn`。
  - 将实时图表包装器抽为 `ui_modules/TauRealtimeChart/TauRealtimeChart.tscn`，固定 tau-plot 节点进入场景。
- UI 模块复用：
  - 主界面复用 `LabelOptionRow`、`LabelSpinRow`、`LabelValueRow`、`StatusBadge`。
  - 新增 `TauRealtimeChart` 模块并补 README。
- 图表插件复用：
  - 实时图表通过 `addons/tau-plot` 的 `TauPlot.Dataset`、`TauXYConfig`、`TauScatterConfig` 渲染，不再自绘坐标轴。
- 本地化：
  - 新增 `locale/ui_translations.csv` 作为表格式翻译源。
  - 新增 `Core/AppLocalization.gd`，运行时读取 CSV 作为兜底翻译。
  - 主界面和通用 UI 模块默认可见文本改为翻译键。
- 反馈入口：
  - 补齐 `Core/ThemeTokens.gd` 和 `Core/UISoundRouter.gd`，使 `ActionButton` 不再引用不存在的核心脚本。
- 文件交互：
  - `FileDialogPanel` 保持绝对路径显示，并在 Godot 支持 `use_native_dialog` 时启用原生文件对话框。
- 类型与风格：
  - 项目自有脚本未发现 `:=`。
  - 修正了图表脚本中未显式标注的关键变量。

## 保留的动态节点

- 推进器实例：运行时数量不确定，由 `ROVController` 动态实例化 `scenes/rov/thruster.tscn`。
- 推进器状态列表行：推进器数量随预设和拼装变化，Dashboard 动态生成只读行。
- tau-plot 数据资源：图表数据和绑定随实时序列变化，由 `TauRealtimeChart` 动态创建资源对象。

以上动态行为符合“运行时数量不确定/数据驱动内容”例外。

## 验证

已执行：

```powershell
.\Godot_v4.6.2-stable_win64_console.exe --path . --log-file .godot\strict_review_test.log --quit-after 3
rg ":=" -n --glob "*.gd" scripts ui_modules Core
rg "[\p{Han}]" scripts ui_modules scenes Core --glob "!*.md" -n
```

结果：

- Godot 启动无脚本/场景错误。
- 自有脚本未发现 `:=`。
- 自有脚本与场景中未发现中文硬编码；中文集中在翻译 CSV 和 README。
- 启动日志存在 Windows 根证书读取错误，未影响项目脚本和场景运行。

